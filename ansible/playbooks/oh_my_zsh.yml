---
- name: Apply Oh My Zsh role only
  hosts: all
  become: true
  gather_facts: true
  module_defaults:
    ansible.builtin.apt:
      lock_timeout: "{{ apt_lock_timeout | default(300) }}"
  roles:
    - role: oh_my_zsh
